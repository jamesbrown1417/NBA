---
title: "NBA Bets"
execute:
  echo: false
  message: false
  warning: false
author: "James Brown"
date: "`r Sys.Date()`"
format:
  html:
    df-print: kable
    theme: cosmo
    self-contained: true
    toc: true
    toc-depth: 3
    fig-width: 8
    fig-height: 6
editor: visual
---

```{r}
# Get the current date and time
current_time <- Sys.time()

# Format the timestamp to be more aesthetic and human-readable
formatted_time <- format(current_time, "%A, %d %B %Y at %I:%M %p")

# Output the formatted timestamp
cat("Generated on:", formatted_time)
```

```{ojs}
viewof odds1 = Inputs.text({
  label: "Odds 1:",
  placeholder: "Enter odds...",
  value: "2.4" // Default value for demonstration
})

viewof stake1 = Inputs.text({
  label: "Stake 1:",
  placeholder: "Enter stake...",
  value: "100" // Default value for demonstration
})

viewof odds2 = Inputs.text({
  label: "Odds 2:",
  placeholder: "Enter odds...",
  value: "1.8" // Default value for demonstration
})

// Format numbers to two decimal places
formatNumber = (value) => parseFloat(value).toFixed(2)

// Format stakes as dollars
formatCurrency = (value) => `$${parseFloat(value).toFixed(2)}`

// Calculate the required stake for arbitrage
stake2 = {
  const o1 = parseFloat(odds1);
  const s1 = parseFloat(stake1);
  const o2 = parseFloat(odds2);
  if (!isNaN(o1) && !isNaN(s1) && !isNaN(o2)) {
    return (s1 * o1) / o2;
  } else {
    return "Please enter valid numerical values.";
  }
}

// Calculate arbitrage percentage (sum of reciprocals of odds minus 1)
arbitragePercentage = {
  const o1 = parseFloat(odds1);
  const o2 = parseFloat(odds2);
  if (!isNaN(o1) && !isNaN(o2)) {
    const arb = (1 / o1) + (1 / o2);
    return -100 * (1 - arb); // Percentage below 1
  } else {
    return "Please enter valid numerical values.";
  }
}

// Calculate total profit (if arbitrage exists)
totalProfit = {
  const o1 = parseFloat(odds1);
  const s1 = parseFloat(stake1);
  const s2 = stake2;
  if (!isNaN(o1) && !isNaN(s1) && typeof s2 === "number") {
    // Profit if Bet 1 wins
    const profit1 = (s1 * o1) - (s1 + s2);
    return profit1; // Both bets yield the same profit
  } else {
    return "Please enter valid numerical values.";
  }
}

// Output the results in an aesthetic format with left-justified columns
html`
  <div style="
    font-family: Arial, sans-serif;
    background-color: #f9f9f9;
    border: 1px solid #ddd;
    border-radius: 8px;
    padding: 20px;
    margin-top: 20px;
    max-width: 600px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  ">
    <div style="display: flex; justify-content: space-between; gap: 20px;">
      <div style="flex: 1; text-align: left;">
        <h3 style="color: #34495e; text-align: left;">Odds</h3>
        <p><strong>Odds 1:</strong> ${formatNumber(odds1)}</p>
        <p><strong>Odds 2:</strong> ${formatNumber(odds2)}</p>
      </div>
      <div style="flex: 1; text-align: left;">
        <h3 style="color: #34495e; text-align: left;">Stakes</h3>
        <p><strong>Stake 1:</strong> ${formatCurrency(stake1)}</p>
        <p><strong>Stake 2:</strong> ${typeof stake2 === "number" ? formatCurrency(stake2) : stake2}</p>
      </div>
    </div>
    <hr style="border: none; border-top: 1px solid #ddd;">
    <h3 style="color: #3498db; text-align: center;">
      Arbitrage Percentage: ${typeof arbitragePercentage === "number" ? formatNumber(-arbitragePercentage) + "%" : arbitragePercentage}
    </h3>
    <h3 style="color: #27ae60; text-align: center;">
      Total Profit: ${typeof totalProfit === "number" ? formatCurrency(totalProfit) : totalProfit}
    </h3>
  </div>
`
```

```{r}
# Libraries
library(tidyverse)
library(DT)
library(quartabs)

# Read in files
all_arbs <- read_rds("../Data/all_arbs.rds")
all_middles <- read_rds("../Data/all_middles.rds")
tab_miss_by_one <- read_rds("../Data/tab_points_miss_by_one.rds")
betright_miss_by_one <- read_rds("../Data/betright_points_miss_by_one.rds")
```

```{r}
# Pass data to OJS
ojs_define(
  all_arbs_data = all_arbs,
  tab_miss_by_one_data = tab_miss_by_one,
  betright_miss_by_one_data = betright_miss_by_one
)
```

:::::: panel-tabset
## Top-Down

```{ojs}
viewof excludeSportsbetUnder = Inputs.toggle({
  label: "Exclude Sportsbet Unders",
  value: false
})
```

::: panel-tabset
## All

```{ojs}
//| column: screen

filtered_all_arbs = {
  const data = transpose(all_arbs_data);
  return data
    .filter(d => d.margin > 0)
    .filter(d => d.over_agency === "Sportsbet" || d.under_agency === "Sportsbet")
    .filter(d => !excludeSportsbetUnder || d.under_agency !== "Sportsbet")
    .map(d => ({...d, margin: Math.round(d.margin * 100) / 100}));
}

html`<style>
.compact-table table { width: 100%; table-layout: auto; font-size: 12px; }
.compact-table th, .compact-table td {
  padding: 4px 6px;
  word-wrap: break-word;
  white-space: normal;
  max-width: 150px;
  overflow-wrap: break-word;
}
.compact-table th {
  background-color: #f0f0f0;
  font-weight: bold;
  position: sticky;
  top: 0;
}
</style>
<div class="compact-table">${Inputs.table(filtered_all_arbs)}</div>`
```

## TAB

```{ojs}
//| column: screen

filtered_tab_arbs = {
  const data = transpose(all_arbs_data);
  return data
    .filter(d => d.margin > 0)
    .filter(d => d.over_agency === "Sportsbet" || d.under_agency === "Sportsbet")
    .filter(d => d.over_agency === "TAB" || d.under_agency === "TAB")
    .filter(d => !excludeSportsbetUnder || d.under_agency !== "Sportsbet")
    .map(d => ({...d, margin: Math.round(d.margin * 100) / 100}));
}

html`<div class="compact-table">${Inputs.table(filtered_tab_arbs)}</div>`
```

## Dabble

```{ojs}
//| column: screen

filtered_dabble_arbs = {
  const data = transpose(all_arbs_data);
  return data
    .filter(d => d.margin > 0)
    .filter(d => d.over_agency === "Sportsbet" || d.under_agency === "Sportsbet")
    .filter(d => d.over_agency === "Dabble" || d.under_agency === "Dabble")
    .filter(d => !excludeSportsbetUnder || d.under_agency !== "Sportsbet")
    .map(d => ({...d, margin: Math.round(d.margin * 100) / 100}));
}

html`<div class="compact-table">${Inputs.table(filtered_dabble_arbs)}</div>`
```

## Dabble Pick Em

```{ojs}
//| column: screen

filtered_dabble_pickem_arbs = {
  const data = transpose(all_arbs_data);
  return data
    .filter(d => d.margin > 0)
    .filter(d => d.over_agency === "Sportsbet" || d.under_agency === "Sportsbet")
    .filter(d => d.over_agency === "Dabble Pickem" || d.under_agency === "Dabble Pickem")
    .filter(d => !excludeSportsbetUnder || d.under_agency !== "Sportsbet")
    .map(d => ({...d, margin: Math.round(d.margin * 100) / 100}));
}

html`<div class="compact-table">${Inputs.table(filtered_dabble_pickem_arbs)}</div>`
```

## Bet365

```{ojs}
//| column: screen

filtered_bet365_arbs = {
  const data = transpose(all_arbs_data);
  return data
    .filter(d => d.margin > 0)
    .filter(d => d.over_agency === "Sportsbet" || d.under_agency === "Sportsbet")
    .filter(d => d.over_agency === "Bet365" || d.under_agency === "Bet365")
    .filter(d => !excludeSportsbetUnder || d.under_agency !== "Sportsbet")
    .map(d => ({...d, margin: Math.round(d.margin * 100) / 100}));
}

html`<div class="compact-table">${Inputs.table(filtered_bet365_arbs)}</div>`
```

## BetRight

```{ojs}
//| column: screen

filtered_betright_arbs = {
  const data = transpose(all_arbs_data);
  return data
    .filter(d => d.margin > 0)
    .filter(d => d.over_agency === "Sportsbet" || d.under_agency === "Sportsbet")
    .filter(d => d.over_agency === "BetRight" || d.under_agency === "BetRight")
    .filter(d => !excludeSportsbetUnder || d.under_agency !== "Sportsbet")
    .map(d => ({...d, margin: Math.round(d.margin * 100) / 100}));
}

html`<div class="compact-table">${Inputs.table(filtered_betright_arbs)}</div>`
```

## BetMakers

```{ojs}
//| column: screen

filtered_betmakers_arbs = {
  const data = transpose(all_arbs_data);
  return data
    .filter(d => d.margin > 0)
    .filter(d => d.over_agency === "Sportsbet" || d.under_agency === "Sportsbet")
    .filter(d => d.over_agency === "BetMakers" || d.under_agency === "BetMakers")
    .filter(d => !excludeSportsbetUnder || d.under_agency !== "Sportsbet")
    .map(d => ({...d, margin: Math.round(d.margin * 100) / 100}));
}

html`<div class="compact-table">${Inputs.table(filtered_betmakers_arbs)}</div>`
```

## Neds

```{ojs}
//| column: screen

filtered_neds_arbs = {
  const data = transpose(all_arbs_data);
  return data
    .filter(d => d.margin > 0)
    .filter(d => d.over_agency === "Sportsbet" || d.under_agency === "Sportsbet")
    .filter(d => d.over_agency === "Neds" || d.under_agency === "Neds")
    .filter(d => !excludeSportsbetUnder || d.under_agency !== "Sportsbet")
    .map(d => ({...d, margin: Math.round(d.margin * 100) / 100}));
}

html`<div class="compact-table">${Inputs.table(filtered_neds_arbs)}</div>`
```

## Pointsbet

```{ojs}
//| column: screen

filtered_pointsbet_arbs = {
  const data = transpose(all_arbs_data);
  return data
    .filter(d => d.margin > 0)
    .filter(d => d.over_agency === "Sportsbet" || d.under_agency === "Sportsbet")
    .filter(d => d.over_agency === "Pointsbet" || d.under_agency === "Pointsbet")
    .filter(d => !excludeSportsbetUnder || d.under_agency !== "Sportsbet")
    .map(d => ({...d, margin: Math.round(d.margin * 100) / 100}));
}

html`<div class="compact-table">${Inputs.table(filtered_pointsbet_arbs)}</div>`
```

## TAB Miss-by-1 Points

```{ojs}
//| column: screen

filtered_tab_miss_by_1_arbs = {
  const data = transpose(tab_miss_by_one_data);
  return data
    .filter(d => d.margin > 0)
    .filter(d => d.over_agency === "Sportsbet" || d.under_agency === "Sportsbet")
    .filter(d => d.over_agency === "TAB" || d.under_agency === "TAB")
    .filter(d => d.line >= 8.5)
    .filter(d => !excludeSportsbetUnder || d.under_agency !== "Sportsbet")
    .map(d => ({...d, margin: Math.round(d.margin * 100) / 100}));
}

html`<div class="compact-table">${Inputs.table(filtered_tab_miss_by_1_arbs)}</div>`
```

## BetRight Miss-by-1 Points

```{ojs}
//| column: screen

filtered_betright_miss_by_1_arbs = {
  const data = transpose(betright_miss_by_one_data);
  return data
    .filter(d => d.margin > 0)
    .filter(d => d.over_agency === "Sportsbet" || d.under_agency === "Sportsbet")
    .filter(d => d.over_agency === "BetRight" || d.under_agency === "BetRight")
    .filter(d => d.line >= 8.5)
    .filter(d => !excludeSportsbetUnder || d.under_agency !== "Sportsbet")
    .map(d => ({...d, margin: Math.round(d.margin * 100) / 100}));
}

html`<div class="compact-table">${Inputs.table(filtered_betright_miss_by_1_arbs)}</div>`
```
:::

## Arbs

::: panel-tabset
## All Arbs

```{r}
#| column: page

# Separate into sportsbet
  all_arbs |>
  filter(margin > 0) |> 
  filter(!over_agency %in% c("Unibet") & !under_agency %in% c("Unibet")) |> 
  mutate(margin = round(margin, 2))
```

## Unibet Arbs

```{r}
#| column: page

unibet_arbs <-
  all_arbs |>
  filter(margin > 0) |> 
  filter(over_agency %in% c("Unibet") | under_agency %in% c("Unibet")) |> 
  mutate(margin = round(margin, 2)) |> 
  arrange(match, desc(margin))

unibet_arbs
```

## Bet365, BetRight, Neds, Dabble 3s

```{r}
#| column: page

dabble_threes_arbs <-
  all_arbs |>
  filter(margin > 0) |> 
  filter(over_agency %in% c("Bet365", "BetRight", "Neds", "Dabble") & under_agency %in% c("Bet365", "BetRight", "Neds", "Dabble")) |> 
  filter(market_name == "Player Threes")

bet365_betright_neds_arbs <-
  all_arbs |>
  filter(margin > 0) |> 
  filter(over_agency %in% c("Bet365", "BetRight", "Neds") & under_agency %in% c("Bet365", "BetRight", "Neds")) |> 
  filter(!over_agency %in% c("Unibet") & !under_agency %in% c("Unibet")) |> 
  bind_rows(dabble_threes_arbs) |>
  mutate(margin = round(margin, 2)) |> 
  arrange(desc(margin))

bet365_betright_neds_arbs
```

## Tab Miss-by-1 Points Arbs

```{r}
#| column: page

tab_miss_by_1_points_arbs <-
  tab_miss_by_one |>
  filter(margin > 0) |> 
  filter(line >= 8.5) |> 
  filter(over_agency %in% c("TAB") | under_agency %in% c("TAB")) |> 
  filter(!over_agency %in% c("Neds", "Unibet") & !under_agency %in% c("Neds", "Unibet")) |> 
  mutate(margin = round(margin, 2))

tab_miss_by_1_points_arbs
```
:::

## Middles

```{r}
#| column: page

all_middles |> 
  mutate(margin = round(margin, 2))
```

## Multi Legs

::: panel-tabset
## Dabble

```{r}
#| results: asis

processed_odds <-
  list.files("../Data/Processed_Odds/", full.names = TRUE) |>
  map_dfr(read_rds) |>
  select(match, player_name, market_name, line, over_price, agency, empirical_prob_last_20) |>
  arrange(desc(empirical_prob_last_20), desc(over_price)) |>
  filter(empirical_prob_last_20 >= 0.9)

# Get list of tibbles (one for each agency)
match_list_dabble <- processed_odds |>
  filter(agency == "Dabble") |>
  select(-agency) |>
  split(~match)

# Add Overall tab for Dabble
overall_dabble <- processed_odds |>
  filter(agency == "Dabble") |>
  select(-agency)
if (nrow(overall_dabble) > 0) {
  match_list_dabble <- c(list(Overall = overall_dabble), match_list_dabble)
}

if (length(match_list_dabble) > 0) {
qreport::maketabs(match_list_dabble, wide = TRUE)}
```

## BetRight

```{r}
#| results: asis

# Get list of tibbles (one for each agency)
match_list_betright <- processed_odds |>
  filter(agency == "BetRight") |>
  select(-agency) |>
  split(~match)

# Add Overall tab for BetRight
overall_betright <- processed_odds |>
  filter(agency == "BetRight") |>
  select(-agency)
if (nrow(overall_betright) > 0) {
  match_list_betright <- c(list(Overall = overall_betright), match_list_betright)
}

if (length(match_list_betright) > 0) {
qreport::maketabs(match_list_betright, wide = TRUE)}
```

## TAB

```{r}
#| results: asis

# Get list of tibbles (one for each agency)
match_list_tab <- processed_odds |>
  filter(agency == "TAB") |>
  select(-agency) |>
  split(~match)

# Add Overall tab for TAB
overall_tab <- processed_odds |>
  filter(agency == "TAB") |>
  select(-agency)
if (nrow(overall_tab) > 0) {
  match_list_tab <- c(list(Overall = overall_tab), match_list_tab)
}

if (length(match_list_tab) > 0) {
qreport::maketabs(match_list_tab, wide = TRUE)}
```

## Neds

```{r}
#| results: asis

# Get list of tibbles (one for each agency)
match_list_neds <- processed_odds |>
  filter(agency == "Neds") |>
  select(-agency) |>
  split(~match)

# Add Overall tab for Neds
overall_neds <- processed_odds |>
  filter(agency == "Neds") |>
  select(-agency)
if (nrow(overall_neds) > 0) {
  match_list_neds <- c(list(Overall = overall_neds), match_list_neds)
}

# only run if there are matches
if(length(match_list_neds) > 0) {
qreport::maketabs(match_list_neds, wide = TRUE)}
```

## Pointsbet

```{r}
#| results: asis

# Get list of tibbles (one for each agency)
match_list_pb <- processed_odds |>
  filter(agency == "Pointsbet") |>
  select(-agency) |>
  split(~match)

# Add Overall tab for Pointsbet
overall_pointsbet <- processed_odds |>
  filter(agency == "Pointsbet") |>
  select(-agency)
if (nrow(overall_pointsbet) > 0) {
  match_list_pb <- c(list(Overall = overall_pointsbet), match_list_pb)
}

# only run if there are matches
if(length(match_list_pb) > 0) {
qreport::maketabs(match_list_pb, wide = TRUE)}
```

## Bet365

```{r}
#| results: asis

# Get list of tibbles (one for each agency)
match_list_bet365 <- processed_odds |>
  filter(agency == "Bet365") |>
  select(-agency) |>
  split(~match)

# Add Overall tab for Bet365
overall_bet365 <- processed_odds |>
  filter(agency == "Bet365") |>
  select(-agency)
if (nrow(overall_bet365) > 0) {
  match_list_bet365 <- c(list(Overall = overall_bet365), match_list_bet365)
}

# only run if there are matches
if(length(match_list_bet365) > 0) {
qreport::maketabs(match_list_bet365, wide = TRUE)}
```
:::
::::::
